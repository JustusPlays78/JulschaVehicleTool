<Window x:Class="JulschaVehicleTool.App.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Password"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource SurfaceBrush}"
        Foreground="{DynamicResource OnSurfaceBrush}">

    <Grid Margin="24,20,24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Prompt -->
        <TextBlock x:Name="PromptText" Grid.Row="0"
                   TextWrapping="Wrap" Margin="0,0,0,16"
                   FontSize="13"
                   Foreground="{DynamicResource OnSurfaceBrush}" />

        <!-- Password field -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Password" FontSize="11"
                       Foreground="{DynamicResource OnSurfaceVariantBrush}"
                       Margin="0,0,0,4" />
            <PasswordBox x:Name="PwdBox"
                         Background="{DynamicResource SurfaceVariantBrush}"
                         Foreground="{DynamicResource OnSurfaceBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         BorderThickness="1"
                         Padding="8,6"
                         KeyDown="OnPasswordBoxKeyDown" />
        </StackPanel>

        <!-- Confirm field (collapsed by default; shown when _confirm=true) -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10"
                    x:Name="ConfirmPanel" Visibility="Collapsed">
            <TextBlock Text="Confirm Password" FontSize="11"
                       Foreground="{DynamicResource OnSurfaceVariantBrush}"
                       Margin="0,0,0,4" />
            <PasswordBox x:Name="ConfirmBox"
                         Background="{DynamicResource SurfaceVariantBrush}"
                         Foreground="{DynamicResource OnSurfaceBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         BorderThickness="1"
                         Padding="8,6"
                         KeyDown="OnPasswordBoxKeyDown" />
        </StackPanel>

        <!-- Error text -->
        <TextBlock x:Name="ErrorText" Grid.Row="3"
                   Foreground="{DynamicResource ErrorBrush}"
                   FontSize="12"
                   Visibility="Collapsed"
                   Margin="0,0,0,12" />

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Foreground="{DynamicResource OnSurfaceVariantBrush}"
                    Margin="0,0,8,0"
                    Padding="16,8"
                    Click="OnCancel" />
            <Button Content="OK"
                    Background="{DynamicResource AccentBrush}"
                    Foreground="White"
                    Padding="24,8"
                    Click="OnOk" />
        </StackPanel>
    </Grid>
</Window>
